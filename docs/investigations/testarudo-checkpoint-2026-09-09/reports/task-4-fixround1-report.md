# Task4 audit round1

Status: nine fixes implemented after observed RED; Task4 real PostgreSQL cases GREEN in root run dpgwaJ. Four old unit fixtures corrected below; module GREEN/typecheck and scoped re-review pending root. Additive migrations written but not applied.

Nine findings accepted. Prepared real PG regressions for historical Payment association without metadata; untrusted socket attribution for ordered/orderless and wrong physical terminal; changed/omitted core replay fields; legacy unresolved terminal availability/admission; retained contract mismatch envelope; bounded recovery beyond oldest200. Unit regressions cover signed handshake mismatch, unsigned claimed proof and downgrade, post-reservation captured socket loss, and watchdog copy. Auth test invokes actual SocketManager connection registration with in-memory fake socket surfaces only.

Adjudicated design: strict core replay compares amount, tip default0, order defaultnull, normalized terminal and tenant on every replay. Old positive PG fixture now includes its original orderId; this was a test omission, not proven released-client contract. Unsigned legacy delivery remains one-shot but conveys no negative financial proof. Signed mismatch cannot register/replay; verified capability cannot bleed across replacement socket/venue. Canonical REST-linked Payment may resolve independently of untrusted socket proof; captured money is preserved even when request attribution is refused. No approximate amount/time correlation.

Tests are scoped to isolated caller-selected codex_testarudo_test_* DB and random tenant cleanup. No shared DB mutation, test execution, migration application, commit, push, deployment or hardware charge.

Observed RED: root ZkUvsw 162 tests /22 failures; Task4 exactly20 behavioral failures (PG15, socket registration3, lostsocket1, watchdogcopy1). All original21 PG cases remained green. Remaining2 failures belong to root-owned work, not these findings.

Implemented nine fixes after RED. Physical admission always uses one short DB transaction: normalized-terminal advisory lock, existing immutable replay validation, physical historical unresolved lookup, then Order lock/sale guard/INSERT. No socket/SDK wait occurs under DB lock. Historical Payment association checked under Payment row lock, independent of optional processor metadata. Socket-only completion requires exact request tag plus TPV Payment source and physical serial; REST remains authoritative and preserves captured money even if attribution is refused. Canonical row JSON retains server-owned mismatch evidence. UNKNOWN sweep uses stable createdAt/id cursor and fixed200 page size; short page wraps next tick. Watchdog copy no longer promises elapsed-time release.

Registry signed mismatch cannot register/replay; unsigned clients retain one-shot legacy delivery without proof. Same existing verified socket/normalized identity/venue survives ordinary updates; null-socket sameidentity samevenue heartbeat preserves but never upgrades proof. Unverified replacement cannot take a verified slot. No heartbeat controller edit needed.

Approved additive indexes added only to TerminalPaymentRequest: paymentId and status/createdAt/id, migration20260909223000_terminal_recovery_attribution_cursor. Not applied. Root coordinates schema map/EXPLAIN. Four old unit replay fixtures now include real mandatory stored contract fields; narrow tx mocks distinguish historical-claim lookup and include Decimal money. Awaiting root module GREEN/typecheck.

Pre-GREEN SQL/budget review: advisory SELECT now casts void to text, matching shared/shiftLifecycleLock.ts. Actual sole caller of getBusyTerminalIds is the live-terminal picker; it now supplies every candidate. Sequential groups of at most100 candidates use PostgreSQL GROUP BY terminalId with take100 (one possible row per candidate), so historical duplicate requests never hydrate into application memory. All candidate batches are traversed; the legacy response retains every live terminal, with no silent limit. Added a201-candidate completeness/budget regression. Root will EXPLAIN against100k synthetic history.

Root verification dpgwaJ: all Task4 real PostgreSQL regressions passed, including historical physical admission, attribution, immutable replay, mismatch preservation and rotating sweep. Four old unit assertions failed because same-venue blocker fixtures omitted venueId (three cases) and the old cross-venue test expected a venue-filtered physical lookup (one case). Fixture-only correction adds the actual tenant on same-venue rows; cross-venue coverage now supplies a real foreign blocker and asserts generic error, hidden request/amount/sender/age/timestamp, physical lookup without tenant filtering, and no insert or emit. No production change in this correction window. Root owns the next module run.
