# Task 5 — implementation report

Status: first financial boundary and canonical receipt implemented after observed RED; verification pending root. Durable outbox engine is test-first stubs, not connected to payment producers yet.

First real PostgreSQL RED prepared in tests/integration/payments/paymentEffectsBoundary.integration.test.ts. Calls public recordOrderPayment using real Prisma; postCashSaleToDrawer throws as a deterministic process-death boundary. Asserts hook reached and Payment/allocation committed before checking paid balances and inventory obligation. Partial-payment regression excludes full-settlement inventory posting. Concurrent real ensureDigitalReceiptResponse recovery must reuse a single stable receipt/accessKey and useful snapshot.

Restricted to caller-supplied local codex_testarudo_test_* database; cleanup only random fixture tenant. No missing imports or missing outbox schema counted as RED. Root owns avq execution; awaiting observed behavioral failure before production changes.

No schema changes, builds, test executions, commits, pushes or production writes performed by this worker.

Root-reported real PostgreSQL RED: run-avoqado-server.DKW1OG confirmed all three behavioral failures: full balance remains PENDING, partial balance remains PENDING, concurrent receipt access keys differ. This is valid boundary evidence; Task5 production remains paused by root while Task3 review fixes take priority.


## Implementation checkpoint (2026-09-09)

After DKW1OG behavioral RED, standalone non-POS/non-area-ticket payments settle canonical paidAmount/remainingBalance/paymentStatus and create the inventory posting plus existing loyalty eligibility marker inside the original Payment transaction. The existing post-commit helper receives the committed settlement outcome and does not overwrite totals in a second transaction. REFUND rows are excluded; historical null Payment.type sales remain included. A new real-PG regression covers that historical case. Fees, drawer, SR APPLY, coupons, and area-ticket behavior remain synchronous as adjudicated.

Digital receipt generation locks the Payment row, reuses the oldest existing receipt, and creates the useful snapshot under the same transaction when absent. Historical duplicates remain intact. No unique migration on legacy receipts.

PaymentEffect schema/Payment+Order+Venue relations are additive, with separate migration 20260909210000_payment_effects_outbox. Schema-map classifier and generated document updated. Migration has NOT been applied by this worker; root owns local fixture application and client generation. Root owns exact terminalPaymentRequestId annotation in closeRowFromPaymentTx; this worker will not modify processorData constructors for that purpose.

Next behavioral RED prepared: tests/integration/payments/paymentEffectsOutbox.integration.test.ts (six cases: commit/restart/frozen dedupe; rollback/tenant validation; expired lease/stale-worker fence; edited review preservation; hard batch25; bounded retry/deadletter/error redaction). paymentEffects.service.ts intentionally contains stubs. Missing generated client/table is setup failure, not RED. Root should generate Prisma/apply this additive migration only in the approved fixture before running. No heavy builds, test commands, migration execution, commits, pushes or deploys by this worker. Scoped Prettier completed; full verification remains pending root.

Remaining implementation: outbox engine after RED, financial-transaction producers for both ordinary and fast paths, idempotent frozen commission snapshot/application, referral/receipt/review handlers, bounded cron lifecycle integration, loyalty net-of-tip parity using the existing marker/job. Do not report Task5 as complete at this checkpoint.


Root verification xL12sj: initial three Task5 real-PG boundary/receipt cases GREEN (and root 14/14 Task4 GREEN). The added legacy-null-type regression failed fixture validation for missing required fee fields; fixed feePercentage/feeAmount/netAmount in it and new outbox fixtures. This was not behavioral RED and did not motivate production changes.

Outbox batch now contains nine cases, including three commission regressions: freeze original rate/recipient despite config change and replay; reserve category base across pending split-payment obligations before workers run; do not invent commission from config created later. Optional transaction-client propagation ownership approved for commission-utils, commission-tier.service and commission-attendance. Commission API remains a stub pending RED. Snapshot goal resolution also calls sales-goal helpers; review that dependency before changing it.

JPKS8t: all four Task5 boundary/receipt cases GREEN. Outbox fixtures failed before assertions because Order.taxAmount was missing; corrected. Audited every fixture model against generated Prisma DMMF required non-default scalar fields (Organization, Venue, Staff, StaffVenue, Order, Payment, Review, CommissionConfig, MenuCategory, Product, OrderItem); remaining fixture data supplies all of them. This schema-only audit executed no database queries. Engine remains stubs pending valid RED.


LJBZrx observed valid engine RED (8 failed/1 negative control passed): zero durable obligations, missing cross-venue rejection, empty bounded claims and zero commission snapshots. Undefined-claim downstream assertions are secondary to missing claims, not separate setup failures. After this evidence, implemented bounded atomic lease claim, six-attempt exhaustion, fenced failures, atomic review/commission effects plus DONE, immutable commission snapshot and reserved pending category base. Root GREEN/typecheck pending. Narrow optional tx passed through existing commission helpers; legacy non-outbox defaults preserved.

Next test-first batch is now paymentEffectsOutbox (14): original nine plus receipt replay, referral no-op replay, real transaction rollback before DONE, tier/read-only goals, and explicit policy failure obligation. Boundary six includes original four GREEN plus ordinary/fast producer RED. New unit payment-effects.job has two RED tests; existing loyalty-reconciliation adds one net-of-tip RED. Worker class is still a stub; producers and receipt/referral handlers are not yet implemented. Goal read-only mode and commission-policy failure isolation await this RED. All scoped edits parsed by eslint with zero errors; formatting completed, one intentionally unused db parameter pending goal read-mode implementation and legacy unused import warning remain. Additional indexes have their own migration 20260909220000_payment_effect_lookup_indexes; no migration execution by worker.


Ium3O3: root relay21 and root unit suites GREEN. Claim returned no due rows under local Mexico session: valid behavioral RED for bare timestamptz binds. Receipt/referral/rollback assertions never reached their handlers and are NOT yet handler RED. Goal read mutated VenueModule (expected0/received1), optional snapshot error rejected financial transaction, order/fast producer arrays empty, job lifecycle and loyalty net-of-tip all produced valid RED.

Implemented after that evidence: utcTs on every raw claim timestamp; two dedicated one-connection session timezone tests; tx-aware read-only goals preserving default legacy callers; savepoint rollback of optional commission policy failure followed by sanitized manual-review obligation; ordinary/fast producers of receipt/review/referral/commission with frozen staff/rating; fast existing loyalty marker; net-of-tip existing loyalty worker; bounded payment-effects job plus start/stop/schedule. Financial expectation for SR/area referral is frozen without changing their synchronous settlement owner. Receipt/referral handlers intentionally await next reachable RED, and pending-tier threshold assertion may now supply remaining RED after read-only goal fix.

Both recordOrderPayment/recordFastPayment now collect monotonic performance.now timings for financial commit, drawer, transaction costs, canonical receipt, SR APPLY, autofactura, and total before normal return. Logs contain identifiers and elapsed times only; measurement/logging cannot throw or run a logger inside the financial transaction. Early idempotent/validation returns still retain their existing request logging; this instrumentation reports the ordinary completed path. Scoped lint has zero errors (legacy console warning and unused catch variable only). Root owns GREEN/typecheck and all DB application; root concurrently owns safe read-only PaymentEffect MCP observability.


Z6Zfvj: original outbox9 GREEN; boundary6 GREEN including both producer crash assertions; job2 and loyalty5 GREEN. Exactly three reachable Task5 failures remained: receipt/referral unsupported handlers and pending-tier second payment snapshot 0.1/tier1 instead of0.2/tier2. Implemented these after RED: consumers run outside effect transaction then CAS completion; committed PAID guard defers SR/area referral until settlement; combined committed+pending commission progress in one MVCC SQL aggregate prevents a delivery transition from counting twice/missing base. Recipient locks sorted before snapshots avoid opposite recipient lock order across simultaneous payments. Legacy commission caller behavior remains default.

New fifteenth outbox test requires sanitized policy reason observable in lastError at enqueue and after failure/exhaustion. Root MCP intentionally excludes payload, making that public reason necessary; reason production is still waiting this focused RED. Dedicated timezone validation is tests/integration/payments/paymentEffectsTimezone.integration.test.ts (2 session cases), which must be included explicitly. Root owns final module/typecheck. Scoped lint zero errors; remaining warning is the existing unused commissionExistsForPayment import.


ZkUvsw: original14 outbox GREEN, timezone2 GREEN under UTC/Mexico, boundary6/job2/loyalty5 GREEN. Public reason test15 produced valid RED (expected COMMISSION_SNAPSHOT_REQUIRES_REVIEW, got null at enqueue). Implemented: mark sanitized public lastError inside fallback transaction, preserve that code on execution failures, and preserve existing sanitized reason during lease exhaustion. The pending and dead-letter rows are now observable without exposing payload through root MCP. This final reason adjustment awaits root GREEN; whole affected module/typecheck and independent review remain pending. No other production changes after this checkpoint until review findings.


Initial independent review accepted five findings; no fixes applied before new RED. Prepared paymentEffectsReview.integration.test.ts (9 real-PG cases): refund before first commission delivery and replay; same-payment category contribution to goal threshold; mixed legacy/TPV tier; no report enrichment in goal policy lookup; actual PostgreSQL SELECT1/0 savepoint recovery with new Payment committed; full refund before pending referral; real coupon issuance/revocation/replay; observed pg_stat_activity Lock race with concurrent refund; and public recordRefund paused after its financial commit before hooks, requiring reversal/reconciliation obligations already present. New boundary seventh case records synthetic CREDIT_CARD with tracked QUANTITY inventory, verifies one paid source/one pending posting line at commit and stable receipt/payment/posting on repeated registration. No processor network involved. Required fixture fields checked with Prisma DMMF; pending root runtime validation.

Proposed minimal handling reuses existing monetary policy: exact processorData.originalPaymentId; commission reversal ratio and original snapshot/recipient unchanged; negative frozen obligation committed with refund; same Order lock across refund/worker/qualification; referral full reversal continues to mean merchandise total excluding tip, partial refunds keep rewards, existing consumed-reward rules unchanged. Pending full-refund referrals become VOID; qualified reward revocation reuses its existing transaction/helper, with durable refund reconciliation obligation instead of a second reward loop. Goal readers will return only required policy thresholds. Root owns all runs and final approval; no round-one production changes at this point.

dpgwaJ: review9 produced seven valid behavioral RED failures (refund commission net, same-payment goal rate, mixed-channel tier, 13 unused report SUM queries, pending referral after full refund, reward/refund lock race, missing two refund-commit obligations). The true PostgreSQL statement-error savepoint and delivered reward revocation/replay controls are GREEN. Original outbox15, boundary7 including card/tracked stock, timezone2, job2, loyalty5 and root read/MCP checks are GREEN. Unrelated Task4 failures in the run remain root-owned. Production correction is paused by root while implementation windows are serialized. Concrete read-only plan: [task-5-round1-fix-plan.md](./task-5-round1-fix-plan.md). No new production edits, tests, builds or DB operations for this checkpoint.
