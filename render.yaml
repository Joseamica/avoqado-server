services:
  - type: web
    name: avoqado-server
    env: node
    plan: free
    region: oregon
    autoDeploy: true
    branch: main
    buildCommand: npm ci --include=dev && npx prisma generate && npm run build npx prisma migrate deploy && node dist/prisma/seed.js
    startCommand: npm start
    healthCheckPath: /health
